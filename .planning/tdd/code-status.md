# Code Status — owned by tdd-coder (feat/routing-sql-push)
_Append-only per task._

| task-id | status | notes |
|---------|--------|-------|
| T1 | green | Added shared `normalize_extensions_filter()` helper in `corpus_forge/backends/base.py` (lowercases, ensures leading dot, raises `ValueError`/`TypeError` for empty / non-string entries). Extended `StorageBackend.chunks_missing_embedding` and added `count_chunks_missing_embedding` to the Protocol with keyword-only `extensions: list[str] \| None = None`. Implemented the SQL push in Postgres (`OR`-joined `lower(COALESCE(d.source_uri, cv.source_uri, '')) LIKE %s` clause appended to the existing JOIN-based query) and SQLite (same shape with `?` placeholders, plus the same JOIN added to `count_chunks_missing_embedding` so the LIKE can reference COALESCE). No JOIN structure changes — the SELECT projection PR #81 wired up is reused verbatim. |
| T2 | green | `corpus_forge/embed.py` `backfill_embedder`: hoisted `_ext_filter = list(embedder.extensions) if embedder.extensions else None` and threaded it through `count_chunks_missing_embedding(..., extensions=_ext_filter)` AND `chunks_missing_embedding(..., extensions=_ext_filter)`. **Deleted the `if not chunks_needing: break` block** — replaced with `continue` (skip this page, fetch the next) so the loop never gives up before `raw_rows == []` signals real end-of-stream. In-memory `route_for` filter kept as defense-in-depth. `corpus_forge/ingest.py` `_write_embeddings_for_chunks`: added a doc-only comment explaining why this call site does not need the SQL filter (chunks come from per-file ingest pipeline, in-memory filter sufficient at batch granularity). |
| T3 | green | No production-code changes for T3 — T3 is integration test scaffolding only; T1's code edits made those tests pass. |
